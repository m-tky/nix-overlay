# Data-science project

Enter the environment with:

```sh
nix develop
```

Then run tools such as `jupyter lab` or `python`.

Edit `flake.nix` to add packages to `extraPackages` or remove standard
packages through `excludePackages`.
